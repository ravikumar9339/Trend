apiVersion: v1
kind: Service
metadata:
  name: trend
  namespace: trend
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  type: LoadBalancer
  selector: { app: trend }
  ports: [{ port: 3000, targetPort: 3000, protocol: TCP }]
